import { CardNameFormatterPipe } from './card-name-formatter.pipe';

describe('CardNameFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CardNameFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
